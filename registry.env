# private registry, eg: docker.myreg.io
export TARGET_REGISTRY=""

# containers authentication file; works for ubuntu; check other linux os
export REGISTRY_AUTH_FILE=/run/user/`id -u`/containers/auth.json
